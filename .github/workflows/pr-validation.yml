name: Validate VOY pull request

# Temporarily manual while the Svelte migration lockfile and gates converge.
# The final exact-head commit restores pull_request validation with frozen install.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  migration-in-progress:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Svelte migration validation is performed by svelte-validation.yml'
