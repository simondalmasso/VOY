# Complete-product Svelte Workers-first release

Active order: `VOY-COMPLETE-END-TO-END-MASTER-02` (`5190243600`).

1. Finish all Svelte product, test, permanent-workflow and documentation changes on `feat/voy-complete-product-master-01`.
2. Remove temporary source-transport and self-committing lockfile workflows.
3. Obtain exact-head PASS for frozen install, release policy, typecheck, lint, deterministic tests, Vite build, bundle budgets, generated Wrangler dry-run and clean browser profiles.
4. Add the controlled final-candidate workflow as the final source commit.
5. Re-read production health and the complete Cloudflare version/deployment/traffic/binding state before mutation; reject drift or ambiguity.
6. Upload exactly one Worker version from the committed Svelte head and assign it `0%`, preserving the verified stable version at `100%`.
7. Validate the Svelte root and build identity, hashed chunks, Vite static manifest, service worker, city datasets, legal routes, Worker APIs, security/privacy, lazy MapLibre/Voice/Auth boundaries and exact-version logs.
8. Validate clean profiles at 360×800, 360×780, 390×844, 412×915, 430×932 and 1280×800; inspect retained screenshots rather than relying only on test status.
9. Require 20 consecutive exact-version rounds and at least 120 seconds of convergence.
10. Persist source SHA, version/deployment IDs, stable/candidate traffic, bindings, rollback identity, asset hashes, browser evidence, sanitized logs, manifest and SHA-256 digest.
11. Publish the same material checkpoint to PR #31 and Issue #30.
12. Stop before productive traffic, Ready transition or merge. Those require independent AUD PASS and a separate exact authorization from Simón.

The final candidate contract contains no dependency on `VOY-Lite.html` or the former V7 monolithic runtime. No source or documentation commit is permitted after candidate creation.
