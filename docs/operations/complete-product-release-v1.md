# Complete-product Workers-first release

Mission: `VOY-COMPLETE-PRODUCT-MASTER-01`.

Permanent controlled workflow: `.github/workflows/master-final-candidate.yml`.

1. Commit the complete product and all release-contract cleanup atomically to `feat/voy-complete-product-master-01`.
2. Run frozen install, release policy, lint, tests, JSON and shell syntax checks, Wrangler dry-run and clean browser validation.
3. Re-read production health, active deployment, all traffic, bindings and non-versioned settings immediately before mutation.
4. Accept one coherent stable version at `100%` and zero-traffic candidates only; reject ambiguous or contradictory traffic.
5. Upload exactly one Worker version from the final exact branch head.
6. Replace the superseded `0%` candidate with one exact-head candidate at `0%`; preserve stable production at `100%`.
7. Validate `/` as runtime-transformed HTML: exact current build identity, Product Shell, Voice product shell, Auth safe-disabled/private-state contract, enforced security headers and hardened session cookie.
8. Preserve exact byte and SHA-256 validation for `/VOY-Lite.html` and the other eleven immutable canonical assets.
9. Require at least twenty consecutive successful exact-version rounds and at least 120 seconds, then validate APIs, territorial schemas, legal pages, PWA, accessibility, desktop/Android/iPhone, Voice, privacy, security and exact-version tail.
10. Persist an immutable artifact containing the exact head, version/deployment IDs, traffic, hashes, browser evidence, logs, rollback identity, manifest and digest.
11. Publish one material checkpoint to PR #31 and Issue #30 for independent AUD.
12. Stop before productive traffic. Promotion requires a new exact human authorization after final AUD PASS.

No production promotion, DNS, secrets, persistent data mutation or feature activation is part of the candidate checkpoint. No head-changing cleanup is permitted after candidate creation; generated identifiers are canonical through the immutable successful artifact and the matching PR #31 / Issue #30 checkpoint.
